# scripts/oversized-allowlist.txt
#
# Files exempt from the size guardrail enforced by
# scripts/check-file-size.sh. Each entry should be removed in the same
# commit as the refactor that brings the file back under threshold.
#
# The allowlist is currently EMPTY — every source file sits under its
# ceiling. Keep it that way: a new entry is an admission of debt and
# its PR description must reference the phase that will retire it.
#
# Lines starting with '#' and blank lines are ignored.
