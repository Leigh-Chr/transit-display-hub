/**
 * Standard page-size options used across all admin data tables.
 * Centralised here so every paginator stays consistent and changes
 * propagate from a single location.
 */
export const ADMIN_PAGE_SIZE_OPTIONS = [10, 25, 50, 100] as const;
