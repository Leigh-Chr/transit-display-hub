/**
 * Pure-function helpers around the domain model — color contrast,
 * translation lookup, service calendar matcher, stop zone resolver.
 * {@code @NullMarked} keeps the helpers consistent with the entities
 * they operate on.
 */
@NullMarked
package com.transit.hub.domain.util;

import org.jspecify.annotations.NullMarked;
