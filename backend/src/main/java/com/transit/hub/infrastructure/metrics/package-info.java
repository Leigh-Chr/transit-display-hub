/**
 * Micrometer-backed metric collectors — GTFS import timings, kiosk
 * heartbeat aggregates. {@code @NullMarked} keeps the metric
 * registration paths consistent with the rest of {@code infrastructure}.
 */
@NullMarked
package com.transit.hub.infrastructure.metrics;

import org.jspecify.annotations.NullMarked;
