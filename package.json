{
  "private": true,
  "name": "transit-display-hub-tooling",
  "description": "Root tooling package for git hooks",
  "license": "AGPL-3.0-only",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1"
  }
}
